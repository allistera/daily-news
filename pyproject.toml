[project]
name = "daily-news"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
    "fastmcp>=3.4.2",
    "jinja2>=3.1.6",
    "resend>=2.32.2",
]

[tool.ruff]
line-length = 100

[tool.ruff.lint]
select = ["E", "F", "W", "I", "B", "UP", "SIM", "C4", "RUF", "BLE"]
